{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --build build --config Debug -j 4",
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}